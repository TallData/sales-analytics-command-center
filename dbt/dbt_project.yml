name: sales_analytics_command_center
version: "1.0.0"
config-version: 2
profile: sales_analytics_command_center
model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
seed-paths: ["seeds"]
clean-targets: ["target", "dbt_packages"]
